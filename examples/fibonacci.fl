// FreeLang v4.1 예제 2: 피보나치 수열 (재귀)

fn fib(n: i32) -> i32 {
  if n <= 1 {
    return n
  }
  fib(n - 1) + fib(n - 2)
}

println("📊 피보나치 수열 (0~10)")
var i: i32 = 0
while i <= 10 {
  println("fib(" + str(i) + ") = " + str(fib(i)))
  i = i + 1
}
